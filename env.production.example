# 复制为 .env 后修改（部署到服务器时使用）
# cp env.production.example .env

# SQLite 路径相对于 prisma/ 目录解析：必须是 file:./dev.db（即 prisma/dev.db）。
# 若写成 file:./prisma/dev.db，会错误指向 prisma/prisma/dev.db，导致读不到仓库里的数据库。
DATABASE_URL="file:./dev.db"
SESSION_SECRET="请改为至少32字符的随机字符串"
ADMIN_SEED_PASSWORD="请改为强密码"
